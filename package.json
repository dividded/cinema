{
  "name": "cinema-project",
  "private": true,
  "scripts": {
    "backend": "cd backend && ./gradlew bootRun",
    "frontend": "cd frontend && yarn dev --host",
    "dev": "concurrently \"yarn backend\" \"yarn frontend\"",
    "backend:test": "cd backend && ./gradlew test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {}
}
